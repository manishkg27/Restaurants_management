const axios = require('axios');
require('dotenv').config({path: '/home/manish/Desktop/Restaurants Management/frontend/.env'});

// We need an auth cookie or token to hit the frontend API.
// Alternatively, we can check the database directly again.
// Let's check the database again.
